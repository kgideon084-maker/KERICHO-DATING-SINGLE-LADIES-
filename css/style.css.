.nav-links.active{
    display:flex;
    flex-direction:column;
    position:absolute;
    top:80px;
    left:0;
    width:100%;
    background:#ffffff;
    padding:25px;
    text-align:center;
    box-shadow:0 10px 20px rgba(0,0,0,.1);
}

.nav-links.active li{
    margin:15px 0;
}
